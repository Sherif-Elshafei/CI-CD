#this workflow will build a python file then 
#run a unit test routine on the file

name: CI
on:
  push: 
    branches: [main]
  pull_request:
    branches: [main]
  
jobs:
  build: 
    runs-on: ubuntu-latest
    run: python circles.py
    
